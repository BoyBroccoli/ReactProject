module.exports = {
    mount: {
        'public':'/',
        'src':'/dist'
    }
}

/**
 * Telling above code to use our public folder as the root of the app.
 * Sets the src directory as the virtual location for the JavaScript files and HTML files
 */